version: '3'

services:
  fines_db:
    image: mysql:8.0.33
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: 'fines_db'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD:
      MYSQL_ROOT_user: 'root'
      MYSQL_ROOT_PASSWORD: 'admin'